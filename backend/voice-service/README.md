# Voice Service (Archived)

This folder is kept for future reference when we add real IndicWhisper integration.

For now, all voice functionality is in the main backend app at `backend/app.py`.

## Future Integration

When ready to add real speech-to-text:
1. Add IndicWhisper model integration
2. Deploy as separate Lambda container
3. Update main app to call this service
